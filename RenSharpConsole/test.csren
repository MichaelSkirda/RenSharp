init 999:
	# x = sys.GetEnv("APPDATA")
	# "{x}"
	# $ print(x)

	python:
		x = 5
		fruits = ["apple", "banana", "cherry"]
		i = 0
		for x in fruits:
			i += 1
			if x == "banana":
				continue
			print(str(i) + " " + x)
		print(config.ScreenWidth)
		config.debug_log("foobar---444")


label main:
	"x is {x} and i is {i}"
	goto expression "test_{"bar_"}123"
	"after test"


label test_bar_123:
	"testtest"
	# Soft return сработает только если был вызван call. Jump проигнорирует soft return.
	soft return
	"foobar"

label test:
	"test1"
	call test_bar_123
	"test2"